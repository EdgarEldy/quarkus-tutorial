# The %test prefix scopes a key to the "test" profile, which @QuarkusTest activates automatically.
# Quarkus's default test port (8081) is often already taken on a developer machine, so tests
# use a dedicated fixed port instead.
%test.quarkus.http.test-port=8085
